# A sample job for Analise_01.

resources:
  jobs:
    analise02:
      name: analise02

      trigger:
        # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
        periodic:
          interval: 1
          unit: DAYS

      #email_notifications:
      #  on_failure:
      #    - your_email@example.com

      parameters:
        - name: catalog
          default: ${var.catalog}
        - name: schema
          default: ${var.schema}

        - task_key: notebook_task
          notebook_task:
            notebook_path: ../src/cliente02.ipynb

      environments:
        - environment_key: default
          spec:
            environment_version: "4"
